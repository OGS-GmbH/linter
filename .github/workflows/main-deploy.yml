name: Main Deploy
on:
  push:
    branches:
      - "main"
  workflow_dispatch:
permissions:
  id-token: write
  contents: write
  pull-requests: write

jobs:
  call-npm-package-deploy:
    uses: OGS-GmbH/reusable-github-actions/.github/workflows/npm-package-publish.yml@main
    secrets:
      NPM_ORG_OGS_GMBH_CI: ${{ secrets.NPM_ORG_OGS_GMBH_CI }}
